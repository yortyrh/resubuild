## 1. Feature flag plumbing

- [x] 1.1 Add `google_oauth: boolean` to the `AuthFeatures` type in `apps/web/src/lib/auth/features.ts`.
- [x] 1.2 Resolve the new flag from `NEXT_PUBLIC_AUTH_GOOGLE_OAUTH_ENABLED` using the same strict-`true` parsing the existing four flags use, and update `getAuthFeatures()` to return the new key.
- [x] 1.3 Add the new key to the `WEB_OPERATOR_CONTROLLED_KEYS` set in `scripts/lib/local-env-composer.mjs` so first-run and re-run behaviour match the existing four flags. The `composeWebEnv` function MUST emit the new `NEXT_PUBLIC_AUTH_GOOGLE_OAUTH_ENABLED` line in the generated `apps/web/.env` body, mirroring how `github_oauth` is written today.
- [x] 1.4 Update `apps/web/src/lib/auth/features.test.ts` to cover the new flag's true/false/empty/non-`true` parsing and to assert the complete five-field shape is returned when every flag is opted in.
- [x] 1.5 Update `scripts/lib/env-prod-schema.mjs` to declare `NEXT_PUBLIC_AUTH_GOOGLE_OAUTH_ENABLED` in the `Web` group of the production manifest, with help text pointing operators to `apps/web/src/lib/auth/features.ts`. Add the new key to the `Web (Next.js)` group in `serializeToDotenv` so it appears in generated `.env.prod` files.

## 2. SPA helper and button

- [x] 2.1 Extend `apps/web/src/lib/auth/oauth.ts` with a `signInWithGoogle()` helper that calls `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: \`\${APP_URL}/auth/callback\` } })`on the publishable-key Supabase client.`APP_URL`MUST be sourced from`window.location.origin`at call time (the same public value the existing`signInWithGitHub`and magic-link flows use). Export a`GOOGLE_OAUTH_ERROR_MESSAGE` constant for the toast fallback.
- [x] 2.2 Create `apps/web/src/components/auth/continue-with-google-button.tsx` exporting a `ContinueWithGoogleButton` component. The component SHALL mirror `ContinueWithGitHubButton` structurally: Sonner error toast on failure, disabled-while-pending state, the same `Separator` + "or" divider, and the same `data-testid`/`aria-label` patterns. The icon SHALL be the `react-icons/si` `Google` (or equivalent) icon to the left of the "Continue with Google" label.
- [x] 2.3 Update `apps/web/src/components/auth/login-form.tsx` to render `<ContinueWithGoogleButton />` after `<ContinueWithGitHubButton />` when `getAuthFeatures().google_oauth` is `true` (above the email/password form, with the existing divider from each button component keeping the visual separation).
- [x] 2.4 Update `apps/web/src/components/auth/register-form.tsx` to render `<ContinueWithGoogleButton />` after `<ContinueWithGitHubButton />` when `getAuthFeatures().google_oauth` is `true` (above the email/password form, mirroring the login-page placement).
- [x] 2.5 Add `apps/web/src/lib/auth/oauth.test.ts` cases for `signInWithGoogle`: the `signInWithOAuth` call shape (provider `google`, `redirectTo: \`\${origin}/auth/callback\``), the `navigated: false`branch when the SDK returns no URL, the error-toast path with the SDK error message, and the fallback to`GOOGLE_OAUTH_ERROR_MESSAGE`when the SDK error has no message. The test file lives beside`oauth.ts` per the colocated-unit-test convention.
- [x] 2.6 Add `apps/web/src/components/auth/continue-with-google-button.test.tsx` (beside the component) covering: renders the default label, invokes `signInWithOAuth` on click and stays on the page on success, surfaces a non-blocking error toast on failure and re-enables the button.
- [x] 2.7 Update `apps/web/src/components/auth/login-form.test.tsx` to extend the existing GitHub button tests with mirror cases for the Google button (hidden when `google_oauth` is off, rendered when on, rendered above the email field when on). Also extend the `setFeatures` helper to include `google_oauth` in its accepted partial type.
- [x] 2.8 Add `apps/web/src/components/auth/register-form.test.tsx` cases for the Google button visibility (mirroring the GitHub coverage that already exists in that file or that gets added in lockstep — verify after reading the current `register-form.test.tsx` before writing).

## 3. Callback handling

- [x] 3.1 Confirm the existing `apps/web/src/app/auth/callback/route.ts` handles the Google PKCE redirect identically to the magic-link and GitHub redirects (no provider branching). The handler at `route.ts` calls `supabase.auth.exchangeCodeForSession(code)` and redirects to `/dashboard` (or `?next=` if present) on success; no code change is required for the Google flow, but the unit test must assert the same code path handles Google-supplied codes.
- [x] 3.2 Add (or extend) a unit test in `apps/web/src/app/auth/callback/route.test.ts` that asserts the Google-supplied code path calls `exchangeCodeForSession` and redirects to `/dashboard` (or the `?next=` param if present), mirroring the existing magic-link and GitHub coverage.

## 4. Supabase provider config

- [x] 4.1 Add an `[auth.external.google]` block to `supabase/config.toml` with `enabled = true` and `client_id = "env(GOOGLE_OAUTH_CLIENT_ID)"`, `secret = "env(GOOGLE_OAUTH_SECRET)"`. Confirm no new entry is required in `[auth].additional_redirect_urls` (the existing `<APP_URL>/auth/callback` entry is reused).
- [x] 4.2 Update `scripts/lib/local-env-composer.mjs` to extend `SUPABASE_OPERATOR_CONTROLLED_KEYS` with `GOOGLE_OAUTH_CLIENT_ID` and `GOOGLE_OAUTH_SECRET`, with the first-run default value of `google-oauth-stub` (parallel to the existing `github-oauth-stub` for GitHub). Update the `composeSupabaseEnv` body and the help comment to document the new keys.
- [x] 4.3 Update `apps/web/.env.example` and `supabase/.env.example` to document the new env vars (`NEXT_PUBLIC_AUTH_GOOGLE_OAUTH_ENABLED` on the web side; `GOOGLE_OAUTH_CLIENT_ID` / `GOOGLE_OAUTH_SECRET` on the Supabase side) with a one-line comment pointing operators to the new `auth-google-oauth` spec.

## 5. Spec and docs

- [x] 5.1 Confirm the new `openspec/changes/implement-google-authentication/specs/auth-google-oauth/spec.md` renders cleanly with `openspec validate implement-google-authentication --strict` (or equivalent) before apply.
- [x] 5.2 Confirm the `web-application` delta at `openspec/changes/implement-google-authentication/specs/web-application/spec.md` and the `auth-feature-flags` delta at `openspec/changes/implement-google-authentication/specs/auth-feature-flags/spec.md` match the source requirements word-for-word under the `## MODIFIED Requirements` / `## ADDED Requirements` headers. Run `openspec validate implement-google-authentication --strict` after the change lands to confirm no drift.
- [x] 5.3 Add a short note to the README (or the `web-application` spec) cross-linking the new `auth-google-oauth` spec and listing the new `NEXT_PUBLIC_AUTH_GOOGLE_OAUTH_ENABLED` env var alongside the existing GitHub and forgot-password / email-verification / passwordless flag notes.

## 6. Verification

- [x] 6.1 Run `pnpm --filter web typecheck` and `pnpm --filter web lint` to confirm no regressions from the new helper, feature flag, and button.
- [x] 6.2 Run the web app's unit tests (`pnpm --filter web test -- --run`) and confirm the new helper, feature flag, button, and callback tests pass alongside the existing auth coverage. _Note: 1 pre-existing failure in `src/lib/_env-probe.test.ts` (localStorage environment issue unrelated to this change) — 492/493 tests pass._
- [x] 6.3 Run the API's unit tests (`pnpm --filter api test -- --run`) and confirm no regressions. The API has no Google-specific code path; this step exists to detect any accidental cross-package import.
- [x] 6.4 Run `pnpm verify` at the root to confirm format, lint, typecheck, and unit tests all pass. _Note: `pnpm verify` node version check fails due to Node v26 on this machine (requires v22); constituent checks (format:check, lint, typecheck, api tests) all pass independently._
- [ ] 6.5 Start the local Supabase stack (`pnpm samples:env && pnpm samples:start`) and confirm Supabase logs that both the GitHub and Google providers are enabled with the stub `client_id`s. _Skipped — local Supabase stack not started in this environment._
- [ ] 6.6 Run the e2e suite (`pnpm test:e2e`) and confirm the new Google callback test passes alongside the existing tests, with no unrelated e2e spec edits. _Skipped — `pnpm test:e2e` requires the local Supabase stack._
- [ ] 6.7 Manually visit `http://localhost:3000/login` with `NEXT_PUBLIC_AUTH_GOOGLE_OAUTH_ENABLED=false` and confirm the Google button does NOT render; flip the flag to `true`, redeploy, and confirm the button renders. _Deferred to operator — manual browser smoke test, not automatable in this environment._

## 7. E2E test impact

- [x] 7.1 **Must pass unchanged** — the new Google helper, button, and `[auth.external.google]` block are additive; no existing e2e test asserts the absence of Google sign-in, and the existing GitHub / magic-link / password / `GET /auth/me` scenarios are unaffected by adding a second external provider. The full e2e catalog from `openspec/specs/e2e-testing/spec.md` (`local-supabase.e2e-spec.ts`) — auth, CV, media, export, template presentation, lifecycle, sections, AI agent, import LLM, import URL, MCP — MUST continue to pass unchanged. The `add-github-login` change established the same constraint for GitHub; this change extends it to Google.
- [x] 7.2 **Update required** — None. No existing e2e scenario is intentionally changing; the OAuth provider list at the Supabase layer grows by one entry (`[auth.external.google]` alongside `[auth.external.github]`), but no e2e scenario asserts the count or membership of that list.
- [x] 7.3 **Add** — None in the API-side Jest e2e suite. The repo does not use Playwright (only the API-side Jest e2e suite under `apps/api/test/e2e/`). The design's intent — button visibility on `/login` and `/register`, callback code consumption, provider flag parsing — is fully covered by colocated unit tests: `apps/web/src/lib/auth/oauth.test.ts` covers the helper; `apps/web/src/components/auth/continue-with-google-button.test.tsx` covers the button; `apps/web/src/components/auth/login-form.test.tsx` and `register-form.test.tsx` cover button visibility on both pages; `apps/web/src/lib/auth/features.test.ts` covers flag parsing; `apps/web/src/app/auth/callback/route.test.ts` covers the Google code path on the callback handler. No new test framework is introduced.
