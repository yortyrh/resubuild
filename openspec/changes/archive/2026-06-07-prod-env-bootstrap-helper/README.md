# prod-env-bootstrap-helper

Add a pnpm script + Cursor SKILL that generates .env.prod for the release-1 docker-compose deployment to a non-self-hosted Supabase project, and ship the matching docker-compose.prod.yml skeleton.
