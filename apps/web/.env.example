# Browser → API over CORS (Bearer JSON)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Supabase is not required in the web bundle for auth. Optional: middleware or
# future SSR features only (keep unset if unused).
