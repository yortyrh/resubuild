import { NewCvLayoutChrome } from './new-cv-layout-chrome';

export default function NewCvLayout({ children }: { children: React.ReactNode }) {
  return <NewCvLayoutChrome>{children}</NewCvLayoutChrome>;
}
